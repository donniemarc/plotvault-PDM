# API access token (optional). If set, the client must supply it.
# Leave empty for no authentication (LAN only, personal use).
API_TOKEN=

# PostgreSQL password for the plotvault_pdm database user (db container + DATABASE_URL)
POSTGRES_PASSWORD=plotvault_pdm

# Host port mapping for the web server
PORT=8642