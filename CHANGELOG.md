# 变更日志

版本号规则（语义化版本）：
- **主版本（X.y.z）**：重大重构/不兼容变更
- **次版本（x.Y.z）**：新增功能
- **补丁（x.y.Z）**：修复缺陷

## [2.0.6] - 2026-09-02

### 客户端
- **修复文件树文件夹与文件双重选中高亮**：点击文件夹内的文件时，文件夹不再保持选中高亮；修复文件夹名称点击事件冒泡导致的重复触发

## [2.0.5] - 2026-09-01

### 后端
- **修复事件监听器内存泄漏**：App.vue 中 blockMiddle 函数提取为模块级函数，添加 onBeforeUnmount 清理 5 个 window 事件监听器
- **修复 FileBrowser.vue 事件监听器泄漏**：提取匿名 dragend 处理为命名函数 onDragEnd，添加 onBeforeUnmount 清理
- **优化批量操作并发执行**：批量删除和移动从串行 for...await 改为 Promise.all 并发执行，提升效率
- **优化 /api/tree API 分页**：新增 TreeQuery 参数（parent_id/page/list_limit），后端新增 list_folders_by_parent 函数
- **改进认证机制**：引入 subtle crate 的 ConstantTimeEq 常量时间比较，防止时序攻击

### 客户端
- **修复 App.vue 事件监听器泄漏**：onBeforeUnmount 中清理所有注册的 window 事件监听器
- **修复 FileBrowser.vue 事件监听器泄漏**：onBeforeUnmount 中清理 dragend 事件监听器
- **优化批量操作并发执行**：批量删除和移动使用 Promise.all 并发执行

## [2.0.4] - 2026-09-01

### 后端
- **修复 drawing_size 列不存在导致上传报错**：将 `drawing_size` 等字段直接写入 CREATE TABLE 建表语句，确保新建数据库包含所有字段；同时保留 ensure_columns 防御性检查

### 客户端
- **修复文件树 Shift+左键多选无反应**：文件夹名称点击事件也绑定 handleFolderClick，确保点击名称区域可触发多选
- **修复属性面板输入框鼠标中键滚动**：使用 window 层级 pointerdown/mousedown/mouseup/auxclick/click 五重拦截，stopImmediatePropagation 确保 Chromium 自动滚动光标不出现

## [2.0.3] - 2026-09-01

### 后端
- **修复 drawing_size 列不存在导致上传报错**：启动时检查 `files` 和 `folders` 表的关键列是否存在，不存在则自动添加，防止迁移失败导致查询报错

### 客户端
- **文件树 Shift+左键多选功能**：支持按住 Shift 键范围选中文件夹
- **全局移除鼠标中键滚动**：禁用浏览器默认的中键自动滚动行为
- **文件树文件节点右键菜单**：文件树中的文件支持右键弹出菜单（预览、下载、重命名、删除）

## [2.0.2] - 2026-08-28

### 后端
- **修复 scan_library_to_db 文件夹重复问题**：`find_folder_by_name` 的 SQL 查询只 SELECT 了 4 个字段，但 `Folder` 结构体有 10 个字段，导致 `sqlx::query_as` 返回错误，每次扫描都创建新文件夹。修复后查询包含所有字段，文件夹去重正常工作。

### 客户端
- 版本号升级到 2.0.2，与后端保持一致

## [2.0.1] - 2026-08-28

### 后端
- **重构 scan_library_to_db 去重机制**：改用 `blob_path`（文件相对路径）进行去重，而非 `name + folder_id`
- 新增 `find_version_by_blob_path` 数据库查询函数
- 彻底解决因 parent_folder_id 不正确导致的重复记录问题

## [2.0.0] - 2026-08-28

### 后端
- **彻底修复文件重复问题**：
  - `finalize_library_file` 和 `move_library_file` 改为直接覆盖已有文件，完全移除 UUID 后缀逻辑
  - 上传时 SHA256 去重：相同内容自动复用已有版本，不创建冗余归档
  - 启动时自动清理 library 中的 UUID 重复文件（优化为数据库查询，避免遍历全部文件）
  - 安全 blob 删除：删除文件/文件夹时检查 blob 是否被其它文件引用
- **修复 scan_library_to_db 返回值**：正确返回新增文件数，而非数据库总文件数

### 客户端
- 版本号升级到 2.0.0，与后端保持一致

## [1.12.2] - 2026-08-28

### 客户端
- 移除搜索框、改名框、新建文件夹框的焦点/激活效果（边框变蓝和双层光环）
- 护眼主题从深绿色调改为暖黄纸质色调（纸质背景、棕色文字、金色强调色）
- 预览面板拖宽时优先保证文件名显示（主区最小宽度从320px提升至762px）
- 文件树内文件支持拖动移动到其他文件夹
- 文件树宽度支持拖拽调整（180px-500px范围）
- 修复右键菜单底部截断问题（自动检测边界并向上翻转）
- **新增属性面板系统**：点击一级目录及以后的文件夹显示文件夹属性，点击文件显示文件属性
  - 属性标签页：编辑文件夹/文件的基本信息（编码、名称、阶段、状态、描述、备注、生成人）
  - 文件列表标签页：显示子文件夹和文件列表，支持导航
  - 版本控制标签页：显示版本历史，支持上传新版本和下载任意版本
  - 操作按钮区：保存属性、重置、上传文件、下载、重命名、删除

### 后端
- 数据库新增字段：code, stage, status, description, remarks, creator（folders和files表）
- 新增API接口：`PATCH /api/folders/{id}/props` 更新文件夹所有属性
- 修改`PATCH /api/files/{id}` 支持所有新字段

## [1.9.7] - 2026-08-28

### 后端
- **修复文件重复导致容量增长**：`finalize_library_file` 改为直接覆盖已有文件，不再创建 UUID 副本
- **上传内容去重**：上传时检查 SHA256，相同内容自动复用已有版本，不创建冗余归档
- **启动自动清理**：扫描 library 目录，自动删除旧逻辑遗留的 UUID 重复文件
- **安全 blob 删除**：删除文件/文件夹时检查 blob 是否被其它文件引用，避免误删共享数据

## [1.12.1] - 2026-08-27

### 客户端
- 移除WebView原生Ctrl+F搜索框（禁用浏览器默认搜索功能）
- 修复检查更新bug：点击下载后未安装，再次检查更新时仍能正确提示需要升级
- 文件树默认展开一级和二级目录，二级以下默认不展开（首次加载时生效）

## [1.12.0] - 2026-08-26

### 客户端
- 重命名弹窗输入框聚焦后光标定位到末尾，不再全选高亮
- 修复文件树三角按钮缺失：文件夹内有文件但无子文件夹时也能展开/收起（原 only 统计子文件夹）

## [1.9.2] - 2026-08-26

### 后端
- 修复移动/重命名大文件时"先更新数据库、后移动磁盘文件"的顺序隐患：若文件移动中途失败（跨卷 copy 中断、源目录被并发删除），数据库不会先指向新位置，避免出现半成品状态导致文件丢失；现改为**先移动磁盘文件、成功后再更新数据库**

## [1.9.1] - 2026-08-26

### 后端
- 修复外部改动导致的"blob file missing"：自动同步增加双向校验，扫描 library/ 时若数据库记录的文件在磁盘上已不存在（用户在 NAS 共享盘直接移动/重命名/删除文件），自动清理该幽灵记录，避免预览/下载报错
- 新增 `db::list_files_by_folder` 按文件夹列出文件

## [1.11.0] - 2026-08-26

### 后端
- 移动/重命名文件时新增同级重名检测（与文件夹行为一致），目标位置已存在同名文件时返回 400 错误

### 客户端
- 修复图片预览时拖拽图片触发上传遮罩的问题（图片不可拖拽）
- 移除预览工具栏中无效的 25%/50%/85% 缩放按钮，仅保留「适应窗口」和「全屏」
- 新建文件夹/重命名弹窗打开后光标自动聚焦到输入框
- 文件树现在可显示当前展开文件夹内的所有文件（带类型标签，点击触发预览）
- 文件树展开状态持久化到 localStorage，重新打开软件自动恢复上次展开的文件夹

## [1.10.0] - 2026-08-26

### 客户端
- 文件树右键菜单增强：文件夹节点新增「重命名」，根节点新增「重命名根目录」
- 移动弹窗目标列表按层级缩进展示，子级文件夹缩进并淡化图标
- 空目录明细区优化：有子文件夹时显示子文件夹卡片网格（点击进入）；完全为空时显示「上传文件」「新建文件夹」快捷按钮
- 修复空目录子文件夹数量显示错误（原用全局总数，现为当前目录直属子文件夹数）
- 全局拦截右键菜单，WebView 原生菜单（刷新/另存为）不再出现在任何区域

## [1.9.0] - 2026-08-25

### 后端
- 新增文件系统自动同步功能：每30秒自动扫描 library/ 目录，将外部新增的文件/文件夹导入数据库
- 新增 `GET /api/sync/status` 接口：返回同步状态（是否正在同步、上次同步时间、同步结果）

### 客户端
- 新增同步状态显示：顶部栏刷新按钮旁显示"同步中"或"已同步"状态
- 优化文件数量显示：根目录/一级目录无文件时不再显示"(0)"
- 优化检查更新体验：检查更新结果显示在按钮旁边，不再与连接设置结果混淆

## [1.8.0] - 2026-08-25

### 客户端
- 新增自动检查更新功能：启动时自动检查GitHub Releases，有更新时设置按钮显示红点提示
- 新增更新检查模块：支持双下载源（GitHub优先，夸克网盘备用）
- 设置页面新增"检查更新"按钮和更新详情展示

## [1.7.8] - 2026-08-24

### 客户端
- 修复文件列表名称列宽度问题：使用固定表格布局，名称列自适应容器宽度

## [1.7.7] - 2026-08-24

### 客户端
- 修复模型预览缩放功能：新增setZoom函数实现绝对缩放，25%、50%、85%按钮现在基于适应状态缩放

## [1.7.6] - 2026-08-24

### 客户端
- 优化文件列表名称列宽度：移除最大宽度限制，名称列自适应内容宽度

## [1.7.5] - 2026-08-24

### 客户端
- 修复模型预览缩放按钮无效问题：移除无效的"＋"和"－"按钮，改为25%、50%、85%比例缩放按钮

## [1.7.4] - 2026-08-24

### 客户端
- 新增护眼模式主题：绿色调护眼配色，减少视觉疲劳

## [1.7.3] - 2026-08-24

### 客户端
- 修复批量下载取消后仍显示成功提示的bug
- 关闭空白区域右键菜单（刷新/另存为）
- 禁止在空白提示区域上传文件（拖放上传）

## [1.7.2] - 2026-08-24

### 客户端
- 批量下载优化：多个文件打包成zip一次性下载，避免多次弹窗
- 文件夹空状态优化：当文件夹包含子文件夹但无文件时，显示友好提示引导用户使用左侧文件树

## [1.7.1] - 2026-08-24

### 客户端
- 文件树右键菜单新增"删除"选项（非根目录）
- 设置页面两列布局
- 根目录欢迎页面：品牌展示 + 数据统计 + 图表 + 最近修改列表

## [1.7.0] - 2026-08-24

### 客户端
- 文件树右键菜单：支持打开文件夹、新建子文件夹
- 批量下载按钮：选中多个文件后可批量下载
- 刷新按钮：文件列表头部新增刷新按钮，点击刷新文件列表和文件夹树
- 阻止空白区域系统右键菜单

### 后端
- 新增 `GET /api/folders/{id}/disk-path` 端点，返回文件夹在 NAS 上的磁盘绝对路径

## [1.6.0] - 2026-08-24

### 客户端
- 文件列表右键菜单：支持打开文件所在文件夹、预览、下载、版本历史、重命名、移动、删除
- 设置页新增"NAS 文件映射路径"输入框，用于配置 SMB 映射盘符路径

### 后端
- 新增 `GET /api/files/{id}/disk-path` 端点，返回文件在 NAS 上的磁盘绝对路径

## [1.5.0] - 2026-08-24

### 客户端
- 文件夹可移动到根目录：文件树根节点（"全部文件"）现在支持拖放接收文件夹

## [1.4.2] - 2026-08-24

### 后端（热修复）
- **修复 folder_path() 死循环**：添加循环检测（HashSet）和最大深度限制（50层），防止数据库中文件夹 parent_id 循环引用导致 CPU 100% + 大量 DB 查询流量

## [1.4.1] - 2026-08-24

### 后端（热修复）
- **修复服务器端口不通**：将 HTTP 端口绑定移到 `scan_library_to_db()` 和 `migrate_to_library()` 之前，服务立即可用，扫描/迁移后台异步执行
- **修复 CPU 占用高 + 大量磁盘 IO**：优化 `scan_library_to_db()` 逻辑，先检查文件是否已存在于数据库，再计算 SHA256，避免每次重启重复读取所有文件

## [1.4.0] - 2026-08-24

### 客户端
- ZIP/RAR 压缩包预览：点击文件显示压缩包内文件树，支持点击下载单个文件
- 文件夹拖拽上传：支持将整个文件夹拖入软件，自动递归收集所有文件上传

### 后端
- 新增 `/api/files/{id}/archive-list` 端点：返回 ZIP/RAR 压缩包内文件列表
- 新增 `/api/files/{id}/archive-entry` 端点：解压并返回压缩包内单个文件
- ZIP 解析使用 `zip` crate（纯 Rust），RAR 解析使用 `unrar` 命令行工具

## [1.3.1] - 2026-08-21

### 客户端
- 点击空白区域关闭预览窗口
- 双击文件夹展开/折叠下级目录
- 设置页面新增版本号显示
- 搜索按钮布局优化

## [1.3.0] - 2026-08-21

### 后端
- 文件夹移动 API：支持修改 `parent_id`，含循环检测与重名检测

### 客户端
- 文件/文件夹拖拽移动：文件拖到文件夹节点直接移动，文件夹拖到文件夹节点改变父级
- 双击打开文件：下载到临时目录并用系统默认程序打开（如 STP 用 UG）
- 应用图标替换为 Drawing Folder 设计

## [1.2.1] - 2026-08-21

### 客户端
- 应用图标替换为 Drawing Folder 设计（文件夹 + 图纸线条 + 钥匙孔）
- 彩色版用于桌面图标，单色版用于标题栏（自动适配主题）

## [1.2.0] - 2026-08-21

### 后端
- 新增启动时自动扫描 `library/` 目录并导入数据库功能（解决数据库清空后重建元数据问题）
- 避免重复上传导致文件重命名浪费空间

## [1.1.0] - 2026-08-20

### 后端
- 项目更名为 PlotVault PDM，镜像/容器/数据库名统一
- 数据库用户/库名改为 `plotvault_pdm`
- converter 容器更名为 `plotvault-pdm-converter`

### 客户端
- 版本统一为 1.1.0
- README 改为中文主页，新增英文版

## [1.0.1] - 2026-08-20

- 服务端启动自动等待数据库（最多 240s 重试）
- compose 不再依赖 healthcheck 门控
- PG 18+ 镜像挂载点修正

## [1.0.0] - 2026-08-20

### 后端（重大重构）
- 数据库 SQLite → PostgreSQL（sqlx 异步连接池）
- config/data 分离，元数据独立存储
- 部署拓扑增加 db 容器

### 客户端
- 项目更名为 PlotVault PDM
- localStorage 键统一为 `plotvault_pdm_*`

## [0.9.0] - 2026-08-20

- 上传进度条改为字节级进度 + 实时网速 + 剩余时间
- 文件树默认展开一级目录
- 修复预览面板与文件列表重叠问题

## [0.8.0] - 2026-08-20

- 批量选择：复选框 + 全选 + 批量移动/删除

## [0.7.0] - 2026-08-20

- 整窗拖放上传
- 预览面板升级（拖拽调宽/全屏/缩放）
- 3MF 模型预览
- STEP/IGES Web Worker 化（不再冻结界面）
- 修复 STL/STEP/3MF 3D 预览崩溃
- 修复 Tauri 拖放上传不生效

## [0.6.0] - 2026-08-19

- 双主题（白天/晚上）+ 顶栏三态切换
- 品牌标识 SVG 化
- 预览区主题联动
- 原生 confirm 替换为应用内模态框

## [0.5.0] - 2026-08-19

### 后端
- config/data 分离，新增 CONFIG_DIR 环境变量
- move_file 跨卷回退 copy

### 客户端
- 新建文件夹后自动展开显示

## [0.4.0] - 2026-08-19

- 文件名重复检测 + 确认弹窗
- 文件树层级连线移除
- 默认服务器地址改为 192.168.1.1:38642

## [0.3.0] - 2026-08-19

### 后端
- NAS 真实目录同步（library/ 真实目录）
- 文件按目录存放，旧版本归档 blobs/
- 启动自动迁移

### 客户端
- 文件树结构重构（根节点"全部文件"）
- 根目录可重命名
- 拖动本地文件到文件树节点直接上传

## [0.2.0] - 2026-08-19

- 文件树默认展开全部层级
- DWG/DXF 预览支持 INSERT/BLOCK 展开
- 大文件上传 body limit 提升到 4GB
- 修复删除文件 JSON 解析错误
- 搭建本机构建环境

## [0.1.0] - 初始版本

- 首个可用版本（Tauri + Vue3 客户端，Rust Axum + SQLite 后端）
